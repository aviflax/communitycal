Our user has provided a plain-English description of an event, which might be recurring.

Here is the description:

> %s

Convert the description into iCalendar format. Respond only with the iCalendar document. Do not wrap it in anything, especially not a Markdown code fence.
