Our user has provided a plain-English description of an event, which might be recurring.

Here is the description:

> %s

Convert the description into iCalendar format.

Include a VTIMEZONE component with the value `%s`.

When constructing the VEVENT:

* Make each property value highly concise
* If the user did not specify the year then assume the current year, even if the start date is in the past
* Omit the parameter TZID from the properties DTSTART and DTEND
* Make sure not to include the parameters `DTSTART` nor `EXDATE` in the property `RRULE`
* Only include a DESCRIPTION property if it would contain additional information that is not in the other properties
  * For example, do not repeat any aspect of the recurrence rule in the description

Respond only with the iCalendar document. Do not wrap it in anything, especially not a Markdown code fence.
