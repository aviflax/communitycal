Our user has provided a plain-English description of an event, which might be recurring.

Here is the description:

> {event-description}

Convert the description into iCalendar format: a VCALENDAR object containing a VEVENT component.

When constructing the VEVENT:

* Make each property value extremely concise
* If the user starts the description with “practice” or “rehearsal” then make that the value of the SUMMARY
* If the user did not specify the year then assume {current-year} even if that would make the start date in the past
* If the user mentioned a location then make sure to include it in a LOCATION property
* Make sure to include the parameter TZID in the properties DTSTART, DTEND, and EXDATE with the value `{timezone-id}`
* Make sure not to include the parameters DTSTART nor EXDATE in the property RRULE
* Only include a DESCRIPTION property if it would contain additional information that is not in the other properties
  * For example, do not repeat any aspect of the recurrence rule in the description
  * Omit DESCRIPTION if it would be blank

Respond only with the iCalendar document. Ensure it contains no blank lines. Do not wrap it in anything, especially not a Markdown code fence.
